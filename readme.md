# k8s-log-generator

Mock log generator for kubernetes.


## Operations

**Build and run**
```sh
make build_and_run
```

**Build for linux**
```sh
make loggen_linux
```

**Build only(default)**
```sh
make loggen
```
